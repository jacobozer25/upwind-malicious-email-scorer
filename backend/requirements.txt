# ─────────────────────────────────────────────────────────────────────────────
# backend/requirements.txt
# Core runtime dependencies for the Malicious Email Scorer backend.
# Pin exact versions in production; use ranges here for flexibility.
# ─────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0

# Settings / validation
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0
email-validator>=2.1.0,<3.0.0

# LLM providers
openai>=1.30.0,<2.0.0
anthropic>=0.26.0,<1.0.0

# HTTP client (shared, with timeouts + retries)
httpx>=0.27.0,<1.0.0

# Redis (rate limiting + semantic cache)
redis[hiredis]>=5.0.0,<6.0.0

# Google auth (ID token verification)
google-auth>=2.29.0,<3.0.0

# Structured logging
structlog>=24.1.0,<25.0.0

# OpenTelemetry (optional tracing hooks)
opentelemetry-api>=1.24.0,<2.0.0
opentelemetry-sdk>=1.24.0,<2.0.0
opentelemetry-instrumentation-fastapi>=0.45b0,<1.0.0

# ─────────────────────────────────────────────────────────────────────────────
# Dev / test dependencies (install with: pip install -r requirements-dev.txt)
# ─────────────────────────────────────────────────────────────────────────────
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
# httpx  (already above — used as async test client)
# ruff>=0.4.0
# black>=24.0.0
# mypy>=1.10.0
# bandit>=1.7.0
# coverage[toml]>=7.5.0
